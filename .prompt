# Set chroot
if [ -z "${debian_chroot:-}" ] && [ -r /etc/debian_chroot ]; then
    debian_chroot=$(cat /etc/debian_chroot)
fi

# Get the active conda environment
conda_env() {
    if [ -n "$CONDA_DEFAULT_ENV" ]; then
        printf "(%s) " "$CONDA_DEFAULT_ENV"
    fi
}

# Get the active branch
parse_git_branch() {
    local branch=$(git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/\1/')
    if [ -n "$branch" ]; then
        if [ -n "$ZSH_VERSION" ]; then
            printf " on %%{\033[01;36m%%}%s%%{\033[00m%%}" "$branch"
        else
            printf " on \033[01;36m%s\033[00m" "$branch"
        fi
    fi
}

# Detect active shell and create prompt
if [ -n "$ZSH_VERSION" ]; then
    setopt PROMPT_SUBST
    PS1='${debian_chroot:+($debian_chroot)}%B%F{green}┌ $(conda_env)%n@%m%f%b:%B%F{blue}%~%f%b$(parse_git_branch)
%B%F{green}└ %#%f%b '
elif [ -n "$BASH_VERSION" ]; then
    PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]┌ $(conda_env)\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]$(parse_git_branch)\n\[\033[01;32m\]└ \$\[\033[00m\] '
fi
